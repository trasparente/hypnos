timezone: Europe/Rome
permalink: pretty
remote_theme: trasparente/mdr@gh-pages

# DEFAULTS
defaults:
  - scope:
      path: ''
    values:
      class: accent-yellow
      layout: default

# COLLECTIONS
collections:
  cronos:
    title: Cronos
    output: true
